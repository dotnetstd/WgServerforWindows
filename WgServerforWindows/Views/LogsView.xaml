<UserControl x:Class="WgServerforWindows.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:WgServerforWindows.Properties"
             xmlns:local="clr-namespace:WgServerforWindows.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <local:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
            
            <!-- Brushes -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="#0078D7"/>
            
            <!-- Styles -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#333333"/>
            </Style>
            
            <Style x:Key="ModernCardStyle" TargetType="Border">
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="3" Opacity="0.2" Direction="270" ShadowDepth="2"/>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style x:Key="ModernButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#0078D7"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center" 
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Opacity" Value="0.8"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="{x:Static properties:Resources.SystemLogs}" 
                   Style="{StaticResource HeaderTextStyle}" 
                   Margin="0,0,0,10"/>
        
        <!-- Controls Row -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton Content="{x:Static properties:Resources.SystemLogs}" 
                            IsChecked="{Binding SelectedLogType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=SystemLogs}" 
                            Margin="0,0,10,0"/>
                <RadioButton Content="{x:Static properties:Resources.OperationLogs}" 
                            IsChecked="{Binding SelectedLogType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=OperationLogs}"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Content="{x:Static properties:Resources.AutoRefresh}" 
                          IsChecked="{Binding IsAutoRefreshEnabled}" 
                          VerticalAlignment="Center" 
                          Margin="0,0,10,0"/>
                <Button Content="{x:Static properties:Resources.Refresh}" 
                        Command="{Binding RefreshLogsCommand}" 
                        Style="{StaticResource ModernButtonStyle}" 
                        Margin="0,0,5,0"/>
                <Button Content="{x:Static properties:Resources.Clear}" 
                        Command="{Binding ClearLogsCommand}" 
                        Style="{StaticResource ModernButtonStyle}"/>
            </StackPanel>
        </Grid>
        
        <!-- Log Content - fills remaining space -->
        <Border Grid.Row="2" Style="{StaticResource ModernCardStyle}" Padding="0">
            <TextBox Text="{Binding LogContent}" 
                     IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas, Courier New, Monospace"
                     FontSize="12"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="10"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"/>
        </Border>
    </Grid>
</UserControl>
