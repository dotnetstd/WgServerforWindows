<UserControl x:Class="WgServerforWindows.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:WgServerforWindows.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Brushes -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="#0078D7"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#005A9E"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="#666666"/>
            
            <!-- Styles -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#333333"/>
            </Style>
            
            <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#333333"/>
            </Style>
            
            <Style x:Key="ModernCardStyle" TargetType="Border">
                <Setter Property="Background" Value="#FFFFFF"/>
                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="3" Opacity="0.2" Direction="270" ShadowDepth="2"/>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style x:Key="ModernButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#0078D7"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center" 
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Opacity" Value="0.8"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="{x:Static properties:Resources.Dashboard}" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,20"/>
            
            <!-- Network Status Card -->
            <Border Style="{StaticResource ModernCardStyle}">
                <StackPanel>
                    <TextBlock Text="{x:Static properties:Resources.NetworkStatus}" Style="{StaticResource SubHeaderTextStyle}"/>
                    
                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel>
                            <TextBlock Text="{x:Static properties:Resources.PublicIpIpv6}" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding PublicIp}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,5"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="{x:Static properties:Resources.MtuOptimizer}" Command="{Binding OpenMtuWizardCommand}" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0" Background="{StaticResource AccentBrush}"/>
                            <Button Content="{x:Static properties:Resources.Copy}" Command="{Binding CopyIpCommand}" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"/>
                            <Button Content="{x:Static properties:Resources.Refresh}" Command="{Binding RefreshIpCommand}" Style="{StaticResource ModernButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Traffic Monitor -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="{x:Static properties:Resources.TrafficActivity}" Style="{StaticResource SubHeaderTextStyle}"/>
                    
                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Static properties:Resources.DownloadRate}" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding DownloadSpeed}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,5"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Static properties:Resources.UploadRate}" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding UploadSpeed}" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,5"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
